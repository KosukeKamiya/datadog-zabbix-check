init_config:

instances:
  - zabbix_user: "<ZABBIX_USERNAME>"
    zabbix_password: ZABBIX_PASSWORD"<>"
    zabbix_api: "<ZABBIX_API_URL>"

    hosts:
      - "<HOSTNAME_1>"
      - "<HOSTNAME_2>"

    metrics:
      - "<ITEM_1>"
      - "<ITEM_2>"
